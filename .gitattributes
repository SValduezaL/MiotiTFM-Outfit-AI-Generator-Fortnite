# Git LFS - Archivos grandes que se gestionarán con LFS

# Modelos LoRA entrenados (necesarios para replicar el proyecto)
# Todos los .safetensors excepto los modelos base grandes (que están en .gitignore)
*.safetensors filter=lfs diff=lfs merge=lfs -text

# Imágenes de ejemplo y resultados (seleccionadas)
4.API_User_Interface/WebUI/**/*.png filter=lfs diff=lfs merge=lfs -text
4.API_User_Interface/WebUI/**/*.jpg filter=lfs diff=lfs merge=lfs -text
4.API_User_Interface/*.png filter=lfs diff=lfs merge=lfs -text
4.API_User_Interface/*.jpg filter=lfs diff=lfs merge=lfs -text
"3.LoRAs/4.Inferencias LoRAs seleccionados/*.png" filter=lfs diff=lfs merge=lfs -text
"3.LoRAs/4.Inferencias LoRAs seleccionados/*.jpg" filter=lfs diff=lfs merge=lfs -text

# Imágenes de datasets LoRAs (para mostrar con qué imágenes se ha trabajado)
"3.LoRAs/1.Datasets LoRAs/**/*.png" filter=lfs diff=lfs merge=lfs -text
"3.LoRAs/1.Datasets LoRAs/**/*.jpg" filter=lfs diff=lfs merge=lfs -text
"3.LoRAs/1.Datasets LoRAs/**/*.jpeg" filter=lfs diff=lfs merge=lfs -text

# Archivos de configuración JSON de ComfyUI (pequeños, no necesitan LFS)
*.json text eol=lf

# Archivos de texto
*.txt text eol=lf
*.md text eol=lf
*.toml text eol=lf

# Scripts Python
*.py text eol=lf

# Notebooks Jupyter
*.ipynb text eol=lf

